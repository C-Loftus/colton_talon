mode: command

-

fix grammar:
    res = user.fix_grammar()
    user.paste(res)

summarize this:
    res = user.summarize_this()
    user.paste(res)

# add context: 
#     res = user.add_context()
#     user.paste(res)
